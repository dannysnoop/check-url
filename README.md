1. **Install dependencies:**

    Using npm:

    ```bash
    npm install
    ```

    Or using yarn:

    ```bash
    yarn install
    ```

2. **Build the project:**

    ```bash
    npm run build
    ```

## Usage

### Running the Service

To start the service in development mode:

```bash
npm run dev